# Local Development Environment Variables
# Copy this file to .env and fill in your actual values

# Demo mode - set to true to use mock data (recommended)
VITE_DEMO_MODE=true

# API Base URL (only needed if not using demo mode)
VITE_API_BASE_URL=http://localhost:3001/api

# Claude AI API Key (optional - for enhanced resume parsing)
# Get your API key from: https://console.anthropic.com/account/keys
# This enables AI-powered skill extraction from resumes
VITE_CLAUDE_API_KEY=

# Content Management Configuration
# Options: 'local', 'mock', 'direct', 'vercel'
# - local: Local JSON-based content (PoC version, default)
# - mock: Legacy mock data (for compatibility)  
# - direct: Direct HubSpot API calls (requires Enterprise account)
# - vercel: HubSpot via Vercel serverless functions (requires Enterprise account)
VITE_HUBSPOT_SERVICE_TYPE=local

# HubSpot API Configuration
# Get this from your HubSpot Private App (starts with 'pat-na1-')
VITE_HUBSPOT_API_KEY=your_hubspot_private_app_token_here

# HubSpot Portal/Hub ID
# Find this in HubSpot Settings > Account & Billing
VITE_HUBSPOT_PORTAL_ID=your_portal_id_here

# HubSpot API Base URL (usually doesn't need to change)
VITE_HUBSPOT_API_BASE_URL=https://api.hubapi.com

# Custom Object Type ID for Dashboard Widget Content
# This will be generated when you create the custom object in HubSpot
VITE_HUBSPOT_CUSTOM_OBJECT_TYPE=2-25634894